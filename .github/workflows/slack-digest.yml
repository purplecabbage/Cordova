steps:
- uses: actions/checkout@v2

- run: mkdir -p path/to/artifact

- run: echo hello > slack-digest/world.txt

- uses: actions/upload-artifact@v2
  with:
    name: my-artifact
    path: path/to/artifact/world.txt
